mixin header(data)
    .header
        .header__container.container-fluid
            .row
                .col
                    a.header__logo.logo(href="#") Beleague
                .col
                    .header__auth
                        a.header__button.button(href="#") войти через steam
